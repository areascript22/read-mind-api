# name: Deploy Production to Heroku

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"

#       - name: Install dependencies
#         run: npm install

#       - name: Build project
#         run: npm run build --if-present

#       # DATABASE_URL específicamente para PRODUCCIÓN
#       - name: Set DATABASE_URL
#         run: echo "DATABASE_URL=${{ secrets.DATABASE_URL_PROD }}" >> $GITHUB_ENV

#       - name: Deploy to Heroku (PRODUCTION)
#         uses: akhileshns/heroku-deploy@v3.13.15
#         with:
#           heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
#           heroku_app_name: ${{ secrets.HEROKU_APP_NAME_PROD }}
#           heroku_email: ${{ secrets.HEROKU_EMAIL }}

#       # Migraciones Prisma en Neon PROD
#       - name: Run Prisma migrations (PROD)
#         run: npx prisma migrate deploy
